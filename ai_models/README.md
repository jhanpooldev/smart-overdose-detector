# Directorio para modelos AI

Compañero:
- En `notebooks/` coloca los Jupyter Notebooks (.ipynb) para el entrenamiento LSTM.
- En `datasets/` coloca los datos crudos de entrenamiento (FC, SpO2).
- En `training_scripts/` coloca los scripts en Python para entrenar y exportar el modelo a `.tflite`.
